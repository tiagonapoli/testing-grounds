import { ProductSizes1, ProductSizes2, ProductSizes3 } from './types'

const fn1 = (productSize: ProductSizes1) => {
    if(productSize === 'BIG') {

    }
}

const fn2 = (productSize: ProductSizes2) => {
    if(productSize === ProductSizes2.BIG) {

    }
}

const fn3 = (productSize: ProductSizes3) => {
    if(productSize === ProductSizes3.BIG) {
        
    }
}